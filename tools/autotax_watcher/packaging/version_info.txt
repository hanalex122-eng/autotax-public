# PyInstaller version resource — Windows EXE'sine gömülen metadata.
# "AutoTaxWatcher.exe sağ tık → Özellikler → Ayrıntılar" sekmesinde görünür.
# Code-signing reputation için doldurulması önemli (Microsoft SmartScreen
# imzasız EXE'leri "publisher unknown" olarak gördüğünde metadata'yı kontrol
# eder; eksik alan bayrak ihtimalini artırır).

VSVersionInfo(
  ffi=FixedFileInfo(
    # filevers ve prodvers TAM 4 sayı olmak zorunda. build.bat üretirken
    # version.txt'den okuyup bu dosyayı yeniden yazar.
    filevers=(2, 0, 0, 0),
    prodvers=(2, 0, 0, 0),
    mask=0x3f,
    flags=0x0,
    OS=0x40004,        # VOS_NT_WINDOWS32
    fileType=0x1,      # VFT_APP
    subtype=0x0,
    date=(0, 0)
  ),
  kids=[
    StringFileInfo(
      [
        StringTable(
          # 040904B0 = US English / Unicode (Windows için en yaygın)
          u'040904B0',
          [
            StringStruct(u'CompanyName', u'AutoTax-Cloud'),
            StringStruct(u'FileDescription', u'AutoTax Scanner Watcher — uploads scanned receipts to AutoTax-Cloud'),
            StringStruct(u'FileVersion', u'2.0.0.0'),
            StringStruct(u'InternalName', u'AutoTaxWatcher'),
            StringStruct(u'LegalCopyright', u'(c) AutoTax-Cloud. All rights reserved.'),
            StringStruct(u'OriginalFilename', u'AutoTaxWatcher.exe'),
            StringStruct(u'ProductName', u'AutoTax Watcher'),
            StringStruct(u'ProductVersion', u'2.0.0.0'),
          ]
        )
      ]
    ),
    VarFileInfo([VarStruct(u'Translation', [1033, 1200])])
  ]
)
